# Sprint 8 — Claude Workflow Improvements

## Context
The workflow rules in CLAUDE.md have been evolving but still have gaps causing items to fall out of sync between `tasks/todo.md` and the `my-site` GitHub Project. Sprint 6 sub-items (6.7-6.19) are still "Todo" in the GitHub Project despite being completed. Additionally, the planning process needs refinement: branches should be created before planning starts, plan files should be named after branches and maintained during planning, and todo/project items should only be created after the plan is finalized with full detail.

## Changes

### 1. Sync existing todo.md items with GitHub Project
- **Sprint 6 items 6.7-6.19**: These are stale sub-tasks from dark mode and profile image work that were completed as part of PRs #16 and #17. Mark all as Done in GitHub Project, or ask user if status is ambiguous.
- **Sprint 7 item 7.1**: Already marked Done — confirmed in sync.
- **Backlog items** (AWS CDK, CI/CD pipeline, MCP integration, Admin UI): Confirm they should remain as Todo.

### 2. Update CLAUDE.md Task Management section
Update the Planning Phase rules to reflect:

1. **Create branch immediately**: When user says "let's plan", create the branch right then (before any planning begins). Branch type (`feature/` or `fix/`) based on context.
2. **Create plan file named after branch**: Create `tasks/plans/<branch-name>.md` and update it throughout planning. If plans change, edit the file accordingly.
3. **Finalize plan file before implementation**: Ensure the plan file has all implementation details before proceeding.
4. **Add todo items after plan is finalized**: Only after planning is complete, add items to `todo.md` with compact but detailed descriptions, then immediately sync to GitHub Project (with matching detail, not just titles).
5. **Keep sync bidirectional**: Any time a todo item is created, updated, or completed in `todo.md`, immediately mirror the change in the GitHub Project.

### 3. Clean up stale plan files
The `/home/jason/.claude/plans/` directory has accumulated plan files from previous sessions. These are auto-generated by Claude Code and not project files — no action needed.

### 4. Update tasks/lessons.md
Add lessons about:
- Always create branch before planning starts
- Plan files should be named after branches and maintained during planning
- GitHub Project items must include detail, not just titles
- Sync todo.md and GitHub Project at every state change

## Files to Modify
- `.claude/CLAUDE.md` — Update Task Management section
- `tasks/todo.md` — Add Sprint 8 items after plan is approved
- `tasks/lessons.md` — Add new lessons
- `tasks/plans/` — New directory for plan files (create `feature-claude-workflow-improvements.md`)

## GitHub Project Sync Actions
Mark Done: 6.7, 6.8, 6.9, 6.10, 6.11, 6.12, 6.13, 6.14, 6.15, 6.16, 6.17, 6.18, 6.19
Confirm remain Todo: AWS CDK infrastructure setup, CI/CD pipeline implementation, MCP integration, Admin UI

## Verification
- All todo.md items match GitHub Project items (status and detail)
- CLAUDE.md rules are clear and unambiguous
- Plan file exists at `tasks/plans/feature-claude-workflow-improvements.md`
- lessons.md updated with new patterns
